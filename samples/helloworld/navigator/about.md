## Navigator

This is a DOMicile example demonstrating popstate handling for SPA behavior with a traditional website. 

Note: Marky is an example of and SPA but it did this by dynamically altering the currently running interface with no URL changes like a traditional SPA.